import {CHANGE_FIELD} from './contants';

const search = {
    filer: ""
}

const reducer = (state =search , action) => {
    switch(action.type) {
        case CHANGE_FIELD: return {

        }
        default: return statenpm 
    }
}


export default reducer;